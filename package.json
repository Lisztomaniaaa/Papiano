{
  "name": "papiano",
  "private": true,
  "description": "Pins the deps bundled into the papiano-shared-deps Lambda layer (see lambda/README.md). Static site + build scripts use npx; the browser app has no bundler.",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0"
  }
}
