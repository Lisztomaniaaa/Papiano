{
  "name": "papiano",
  "private": true,
  "description": "Vercel-only runtime deps. Static site + build scripts use npx; the browser app has no bundler.",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "firebase-admin": "^12.7.0"
  }
}
